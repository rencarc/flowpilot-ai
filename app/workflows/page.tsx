import { AppShell, PageHeader, Panel, Tag } from "@/components/ui";
import { workflowTemplates } from "@/lib/mock-data";

export default function WorkflowsPage() {
  return <AppShell><PageHeader title="Workflows" subtitle="Approved templates and AI proposals. AI can suggest, but only approved active templates can execute." /><div className="tabs"><button className="tab active">Approved Templates</button><button className="tab">AI Proposals</button></div><div className="workflow-grid"><Panel title="Approved templates" tag={<Tag tone="approved">3 active</Tag>}>{workflowTemplates.map((template) => <article className="template-card" key={template.id}><div className="row-between"><h3>{template.name}</h3><Tag tone="approved">{template.version}</Tag></div><p>{template.category} / {template.handoffTarget}</p><div className="pill-list">{template.riskGates.map((gate) => <Tag key={gate}>{gate}</Tag>)}</div></article>)}</Panel><Panel title="AI proposals" tag={<Tag tone="review">Draft only</Tag>}><article className="template-card"><h3>Vendor Security Review</h3><p>Drafted from a no-template-match intake. Requires admin approval before becoming executable.</p><div className="kv"><div className="kv-item"><span>Status</span><strong>under_review</strong></div><div className="kv-item"><span>Limitation</span><strong>Cannot execute directly</strong></div></div></article></Panel></div></AppShell>;
}
